# Done tickets

Completed tickets are moved here (`git mv`) by the PR that completes them,
with `status: done`, a `completed:` date and filled-in Completion notes.
Together they form the project's changelog of what was built and why.
