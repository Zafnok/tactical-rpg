[alias]
xtask = "run -q -p xtask --"

# rustup's self-contained x86_64-pc-windows-gnu linker ships without a full
# mingw-w64 import library set (e.g. no libimm32.a, which macroquad needs for
# IME support). Use a full MSYS2 mingw-w64 toolchain instead. Install with:
#   winget install -e --id MSYS2.MSYS2
#   C:\msys64\usr\bin\pacman.exe -S mingw-w64-x86_64-gcc
[target.x86_64-pc-windows-gnu]
linker = "C:/msys64/mingw64/bin/gcc.exe"
ar = "C:/msys64/mingw64/bin/ar.exe"

# `quad-storage` (web storage, ticket 0207) is a miniquad JS plugin: its
# `extern "C"` functions are implemented in JS and loaded into the page
# (index.html) at runtime, not linked in. Without this flag, rust-lld treats
# them as link errors instead of leaving them as wasm imports.
[target.wasm32-unknown-unknown]
rustflags = ["-C", "link-arg=--allow-undefined"]
