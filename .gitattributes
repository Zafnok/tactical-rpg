# Normalise text to LF everywhere. ASCII maps and portraits depend on exact
# column alignment, so line endings must never be rewritten per-platform.
* text=auto eol=lf

*.png  binary
*.ico  binary
*.ttf  binary
*.otf  binary
*.wasm binary
*.ogg  binary
*.wav  binary
